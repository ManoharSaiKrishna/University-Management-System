- pip install streamlit -> to install streamlit

- streamlit run main.py -> to run the code

if it gives an error "streamlit not found"

- python -m streamlit run main.py

>> Uploading the project on GITHUB

Step-1: Create a repository on GITHUB
Step-2: In gitbash change to your project directory using 'cd'
Step-3: Clone the repository
        ( git clone URL_of_repository )
